#cloud-config
system_info:
  default_user:
    name: rocky
    home: /home/rocky
    sudo: ALL=(ALL) NOPASSWD:ALL
    passwd: ""
    lock_passwd: false
    shell: /bin/bash
hostname: saga-base
ssh_pwauth: False
